// Ganjil Atau Genap?

/*
 * Akan diberikan sebuah angka, silahkan tentukan apakah angka tersebut ganjil atau genap.
 */

const input = 7.001
const output = input % 2 == 0 ? 'Genap' : 'Ganjil'
console.log(output)